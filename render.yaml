services:
  - type: web
    name: recalldeck
    env: node
    buildCommand: npm install && npm install --prefix client --include=dev && npm run build --prefix client
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
